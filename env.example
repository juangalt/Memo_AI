## Example Environment Configuration for Memo AI Coach
# Copy to .env and adjust values as needed.

# --- Primary ---
# CLAUDE_API_KEY: Anthropic Claude API key for real evaluations.
# - Default when unset: mock mode (no external LLM calls).
# - Example: sk-ant-api... (do NOT commit real keys)
CLAUDE_API_KEY=

# APP_ENV: Runtime environment (affects logging defaults and env-specific settings).
# - Allowed: development | production | staging
# - Default when unset: production
APP_ENV=production

# DOMAIN: Public hostname for routing and frontend config.
# - Used by Traefik rules and backend `/api/v1/config/frontend`.
# - Default when unset: localhost
DOMAIN=localhost

# --- Recommended ---
# ADMIN_EMAIL: Contact email used by Traefik Let's Encrypt.
# - Default when unset: admin@example.com (Traefik fallback)
# ADMIN_EMAIL=admin@your-domain.com

# --- Overrides (these override YAML/runtime defaults) ---
# ADMIN_PASSWORD: Initial admin password for database initialization.
# - Default when unset: admin123 (on first init). If set later, init script updates admin password.
# ADMIN_PASSWORD=change-this-in-production

# SESSION_TIMEOUT: Session expiration in seconds.
# - Overrides config/auth.yaml: session_management.session_timeout
# - Default when unset: value from config/auth.yaml (3600 by default)
# SESSION_TIMEOUT=3600

# DATABASE_URL: Database connection string.
# - Overrides code default for local runs; compose currently sets `sqlite:///data/memoai.db` directly.
# - Default when unset: sqlite:///data/memoai.db
# DATABASE_URL=sqlite:///data/memoai.db

# --- Optional Logging Overrides ---
# DEBUG: If set (any value), forces DEBUG log level.
# DEBUG=1

# LOG_LEVEL: Explicit log level (DEBUG, INFO, WARNING, ERROR, CRITICAL).
# - Ignored if DEBUG is set. Defaults based on APP_ENV.
# LOG_LEVEL=INFO

# LOG_FILE: Path to log file. If unset, logs go to console only.
# LOG_FILE=logs/app.log

# LOG_FORMAT: Custom Python logging format string.
# LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s
